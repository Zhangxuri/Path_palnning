<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heitian.ssm.dao.ProductListDao">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="com.heitian.ssm.model.ProductList">
        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="product" property="product" jdbcType="BIGINT"/>
        <result column="sendname" property="sendname" jdbcType="VARCHAR"/>
        <result column="sendtel" property="sendtel" jdbcType="BIGINT"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="Base_Column_List">
        id,
        address,
        product,
        sendname,
        sendtel
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="productList.id">
        INSERT INTO product_list (
            id,
            address,
            product,
            sendname,
            sendtel
        ) VALUES (
            #{productList.id,jdbcType=BIGINT},
            #{productList.address,jdbcType=VARCHAR},
            #{productList.product,jdbcType=BIGINT},
            #{productList.sendname,jdbcType=VARCHAR},
            #{productList.sendtel,jdbcType=BIGINT}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="productList.id">
        INSERT INTO product_list
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productList.id!=null"> id,</if>
            <if test="productList.address!=null"> address,</if>
            <if test="productList.product!=null"> product,</if>
            <if test="productList.sendname!=null"> sendname,</if>
            <if test="productList.sendtel!=null"> sendtel,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="productList.id!=null">#{productList.id,jdbcType=BIGINT},
            </if>
            <if test="productList.address!=null">#{productList.address,jdbcType=VARCHAR},
            </if>
            <if test="productList.product!=null">#{productList.product,jdbcType=BIGINT},
            </if>
            <if test="productList.sendname!=null">#{productList.sendname,jdbcType=VARCHAR},
            </if>
            <if test="productList.sendtel!=null">#{productList.sendtel,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO product_list (
        <include refid="Base_Column_List"/>
        )VALUES
        <foreach collection="productLists" item="productList" index="index" separator=",">
            (
            #{productList.id,jdbcType=BIGINT},
            #{productList.address,jdbcType=VARCHAR},
            #{productList.product,jdbcType=BIGINT},
            #{productList.sendname,jdbcType=VARCHAR},
            #{productList.sendtel,jdbcType=BIGINT}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE product_list
        <set>
            <if test="productList.id != null"> id= #{productList.id,jdbcType=BIGINT},</if>
            <if test="productList.address != null"> address= #{productList.address,jdbcType=VARCHAR},</if>
            <if test="productList.product != null"> product= #{productList.product,jdbcType=BIGINT},</if>
            <if test="productList.sendname != null"> sendname= #{productList.sendname,jdbcType=VARCHAR},</if>
            <if test="productList.sendtel != null"> sendtel= #{productList.sendtel,jdbcType=BIGINT}</if>
        </set>
        WHERE id = #{productList.id,jdbcType=BIGINT}
    </update>

<!--auto generated by codehelper on 2018-01-19 14:47:37-->
	<select id="find" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from product_list
    </select>

<!--auto generated by codehelper on 2018-01-19 15:35:23-->
	<select id="findProduct" resultType="java.lang.Long">
        select product
        from product_list
    </select>
</mapper>

