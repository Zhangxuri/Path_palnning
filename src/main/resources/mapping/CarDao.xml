<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heitian.ssm.dao.CarDao">
    <!--auto generated Code-->
    <resultMap id="BaseResultMap" type="com.heitian.ssm.model.Car">
        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="volum" property="volum" jdbcType="DOUBLE"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="tele" property="tele" jdbcType="BIGINT"/>
    </resultMap>

    <!--auto generated Code-->
    <sql id="Base_Column_List">
        id,
        volum,
        name,
        tele
    </sql>

    <!--auto generated Code-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="car.id">
        INSERT INTO car (
            id,
            volum,
            name,
            tele
        ) VALUES (
            #{car.id,jdbcType=BIGINT},
            #{car.volum,jdbcType=DOUBLE},
            #{car.name,jdbcType=VARCHAR},
            #{car.tele,jdbcType=BIGINT}
        )
    </insert>

    <!--auto generated Code-->
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="car.id">
        INSERT INTO car
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="car.id!=null"> id,</if>
            <if test="car.volum!=null"> volum,</if>
            <if test="car.name!=null"> name,</if>
            <if test="car.tele!=null"> tele,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="car.id!=null">#{car.id,jdbcType=BIGINT},
            </if>
            <if test="car.volum!=null">#{car.volum,jdbcType=DOUBLE},
            </if>
            <if test="car.name!=null">#{car.name,jdbcType=VARCHAR},
            </if>
            <if test="car.tele!=null">#{car.tele,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>

    <!--auto generated Code-->
    <insert id="insertList">
        INSERT INTO car (
        <include refid="Base_Column_List"/>
        )VALUES
        <foreach collection="cars" item="car" index="index" separator=",">
            (
            #{car.id,jdbcType=BIGINT},
            #{car.volum,jdbcType=DOUBLE},
            #{car.name,jdbcType=VARCHAR},
            #{car.tele,jdbcType=BIGINT}
            )
        </foreach>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE car
        <set>
            <if test="car.id != null"> id= #{car.id,jdbcType=BIGINT},</if>
            <if test="car.volum != null"> volum= #{car.volum,jdbcType=DOUBLE},</if>
            <if test="car.name != null"> name= #{car.name,jdbcType=VARCHAR},</if>
            <if test="car.tele != null"> tele= #{car.tele,jdbcType=BIGINT}</if>
        </set>
        WHERE id = #{car.id,jdbcType=BIGINT}
    </update>

<!--auto generated by codehelper on 2018-01-19 15:43:16-->
	<select id="findbyVolumgreaterThan" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from car
        where volum<![CDATA[>]]> #{minVolum,jdbcType=DOUBLE}
    </select>
</mapper>

